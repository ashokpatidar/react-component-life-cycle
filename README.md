# how to use component lifecycle methods in React.js.

Lifecycle method used in react and in which order.

Also there is possible that a functional component can have the class based component

# http-server ./build